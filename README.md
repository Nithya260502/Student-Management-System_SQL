# Student-Management-System_SQL

The Student Management System is a database project built using MySQL to manage and organize student, instructor, course, enrollment, and grade information efficiently.

It demonstrates core SQL concepts such as table creation, relationships (foreign keys), joins, views, triggers, stored procedures, indexing, subqueries, and aggregate functions.
